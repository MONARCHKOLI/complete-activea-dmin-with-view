<%# <%= @screening.severity%>



<%= link_to "Home" , root_path%>

<%= form_for(:answer, url:  new_screening_question_answer_path, method: :post) do |f| %>
  <% @question.each_with_index do |question,index|%>
    <br>
    <br>
    <%= f.label question.question %>
    <br>
    <br>
    <%# <%= f.select(@answer, Screening::SCORE.keys, id: "response-#{index}") %> %>
  <% end %>
  <br>
  <br>
  <br>
  <br>
  <br>
  <%= f.submit "Complete Screening" %>
<% end %>

<%# <%= link_to "Screen Again", check_in_path(params[:check_in_id])%>